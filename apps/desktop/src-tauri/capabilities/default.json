{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window loads the hosted app and may hand external links to the system browser.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
