/** Placeholder — replaced by the full demo seed in the feature build. */
export async function seedDemoAccount(): Promise<{ email: string; items: number }> {
  return { email: "demo@clover.local", items: 0 };
}
