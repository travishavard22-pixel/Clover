/**
 * Colours baked into image pixels (rings, labels, default paper). Image files cannot read CSS
 * variables, so these mirror the brand tokens in docs/brand/brand-identity.md §4.1.
 */
export const STUDIO_ACCENT_HEX = "#1E7A4C"; // --clover-green
export const STUDIO_PAPER_HEX = "#F8F7F3"; // --clover-paper / --studio-bg (light)
export const STUDIO_INK_HEX = "#141613"; // --clover-ink
